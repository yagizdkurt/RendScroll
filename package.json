{
  "name": "rendscroll",
  "private": true,
  "description": "RendScroll — TTRPG scene reader/renderer.",
  "scripts": {
    "test": "node --test && python -m unittest discover -s test -p \"test_*.py\""
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
