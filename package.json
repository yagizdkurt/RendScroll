{
  "name": "rendscroll",
  "private": true,
  "description": "RendScroll — TTRPG scene reader/renderer.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
